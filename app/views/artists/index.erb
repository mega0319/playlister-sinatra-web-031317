<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Artists</title>
  </head>
  <body>
    <h1>Click on an Artist!</h1>
    <% @artists.each do |artist| %>
      <li><a href="/artists/<%=artist.slug %>"><%= artist.name %></a></li>
      <% end %>
  </body>
</html>
