<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Genres</title>
  </head>
  <body>
    <h1>Click on a Genre!</h1>
    <% @genres.each do |genre| %>
      <li><a href="/genres/<%=genre.slug %>"><%= genre.name %></a></li>
      <% end %>

  </body>
</html>
